export class UpdateUserCondition {
    nUpdateUserIDMust: number; // (操作用户ID)
    sUpdateUserNameMust: string; // (操作用户)
    nUserID: number; // (用户ID)
    sTel: string; // (联系方式)	
    sWorkCode: string; // (人员编号)
    sUserName: string; // (人员姓名)
    sPost: string; // (岗位)
    sRemark: string; // (备注)
    nState: number  // 修改的状态
    sNewPassword: string;
    sOldPassword: string;
}