export class SearchLogCondition {
    nSearchUserIDMust: number; // (操作用户ID)
    sSearchUserNameMust: string; // (操作用户)
    nUserID: number; // (用户ID)
    sWorkCode: string; // (人员编号)
    sUserCode: string; // (用户账号)
    sUserName: string; // (用户姓名)
    nOperaType: number; // (操作类型)
    sPost: string; // (岗位)
    sDateStart: string; // (开始时间)
    sDateEnd: string; // (结束时间)
}
