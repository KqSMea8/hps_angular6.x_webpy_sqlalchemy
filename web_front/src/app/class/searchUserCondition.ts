export class SearchUserCondition {
    nSearchUserIDMust: number; // (操作用户ID)
    sSearchUserNameMust: string; // (操作用户)
    nUserID: number; // (用户ID)
    sUserCode: string; // (用户名)
    sTel: string; // (联系方式)	
    sWorkCode: string; // (人员编号)
    sUserName: string; // (人员姓名)
    nState: number;  // 状态
    sPost: string; // (岗位)
    sCreateTimeStart: String; // (创建时间开始)
    sCreateTimeEnd: String; // (创建时间结束)
}