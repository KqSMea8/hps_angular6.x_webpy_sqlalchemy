import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { HotelOfflineComponent } from './hotel-offline.component';

describe('HotelOfflineComponent', () => {
  let component: HotelOfflineComponent;
  let fixture: ComponentFixture<HotelOfflineComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ HotelOfflineComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(HotelOfflineComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
