export class DataRole {
    DataRoleID: number;  // (权限ID)
    DataTypeID: number;  // (权限名ID)
    DataTypeName: string;  // (权限名，表名)
    Fields: string;  // (权限，对应的字段)
    FieldType: number;  // (权限类型，字段的类型)
    FieldTypeName: string;  // (权限类型名称，字段的类型)
    Remark: string;  // (备注)
    BeginValue: string;  // 范围开始值
    CreateTime: string;  // 创建时间
    EndValue: string;  // 范围结束值
    Flag: number;  // 标识符
    UserDataRoleFlag: number;  // 标识符
    UserDataRoleID: number;  // 用户权限关联表ID
    ValueSet: string;  // 值得范围
}
